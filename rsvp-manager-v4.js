const RSVP_KEY="event-report-workspace-v1";
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??"").replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const norm=v=>String(v??"").trim().toLowerCase().replace(/[\s_\-\/()：:]/g,"");
const yes=v=>["v","yes","y","true","1","是","有","已出席","attended","checkedin"].includes(norm(v));
const walk=v=>/walk\s*-?\s*in|現場報名/i.test(String(v??""));
const rid=()=>`rsvp-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,7)}`;
const ALIASES={notes:["備註","remark","remarks","note","notes"],attended:["出席","出席狀況","attendance","attended","checkin","checkedin"],survey:["問卷","survey"],code:["編號","no","number","id"],mco:["mco"],type:["type","類別","category","attendeetype"],name:["姓名","name","fullname","attendeename"],company:["中文公司名稱","公司","company","companyname"],title:["中文職稱","職稱","title","jobtitle"],department:["部門別","部門","department"],jobLevel:["職位別","職位","joblevel","seniority"],decisionRole:["決策角色","decisionrole","role"],email:["電子信箱","電子郵件","email","mail"],companyPhone:["公司電話","officephone","companyphone"],extension:["分機","ext","extension"],phone:["行動電話","手機","mobile","phone"],registrationSource:["報名資訊來源","registrationsource","invitesource"],consentContact:["同意連絡","同意聯絡"],consentPrivacy:["同意使用個資","個資同意"],submittedAt:["填寫時間","submittedat","timestamp"],reserved:["保留欄位"],source:["來源","source"],confirmPhone:["確認電話"]};
function canon(v){const n=norm(v);for(const[k,a]of Object.entries(ALIASES))if(a.some(x=>norm(x)===n))return k;return null;}
function headerRow(matrix){let best={i:-1,score:0};for(let i=0;i<Math.min(20,matrix.length);i++){const keys=(matrix[i]||[]).map(canon).filter(Boolean),must=keys.includes("name")&&(keys.includes("type")||keys.includes("code")||keys.includes("company")),score=keys.length+(must?20:0);if(score>best.score)best={i,score};}return best.score>=22?best.i:-1;}
function parseSheet(sheetName,sheet){const matrix=XLSX.utils.sheet_to_json(sheet,{header:1,defval:"",raw:false}),h=headerRow(matrix);if(h<0)return[];const headers=matrix[h].map(canon),out=[];for(let r=h+1;r<matrix.length;r++){const src=matrix[r]||[],x={};headers.forEach((k,c)=>{if(k)x[k]=src[c]??""});const name=String(x.name||"").trim(),code=String(x.code||"").trim(),email=String(x.email||"").trim();if(!name&&!code&&!email)continue;const notes=String(x.notes||"").trim(),walkin=walk(notes),type=String(x.type||sheetName||"").trim();out.push({id:code||rid(),code,type,name,company:String(x.company||"").trim(),title:String(x.title||"").trim(),department:String(x.department||"").trim(),jobLevel:String(x.jobLevel||"").trim(),decisionRole:String(x.decisionRole||"").trim(),email,companyPhone:String(x.companyPhone||"").trim(),extension:String(x.extension||"").trim(),phone:String(x.phone||"").trim(),registrationSource:String(x.registrationSource||"").trim(),source:String(x.source||"").trim(),notes,mco:String(x.mco||"").trim(),consentContact:String(x.consentContact||"").trim(),consentPrivacy:String(x.consentPrivacy||"").trim(),submittedAt:String(x.submittedAt||"").trim(),reserved:String(x.reserved||"").trim(),confirmPhone:String(x.confirmPhone||"").trim(),attended:yes(x.attended)||walkin,walkin,survey:yes(x.survey),importSheet:sheetName});}return out;}
function keyOf(x){return x.code?`code:${String(x.code).toLowerCase()}`:x.email?`email:${String(x.email).toLowerCase()}`:`person:${String(x.type)}|${String(x.name)}|${String(x.company)}`.toLowerCase();}
function dedupe(rows){const m=new Map();for(const x of rows){const k=keyOf(x);if(!m.has(k))m.set(k,x);else{const o=m.get(k);m.set(k,{...o,...x,id:o.id||x.id,attended:o.attended||x.attended,walkin:o.walkin||x.walkin,survey:o.survey||x.survey});}}return[...m.values()];}
function readWS(){try{return JSON.parse(localStorage.getItem(RSVP_KEY)||"null")}catch{return null}}
function project(ws=readWS()){return(ws?.projects||[]).find(p=>p.id===ws.activeId)||null}
function write(mut){const ws=readWS(),p=project(ws);if(!p)return false;p.attendees=Array.isArray(p.attendees)?p.attendees:[];mut(p.attendees,p);localStorage.setItem(RSVP_KEY,JSON.stringify(ws));return true;}
function role(){return($("#userRole")?.textContent||"VIEWER").trim().toLowerCase();}
function editable(){return role()!=="viewer";}
function refresh(){setTimeout(renderManager,10);}
async function importWorkbook(file){const book=XLSX.read(await file.arrayBuffer(),{type:"array"});let incoming=[];for(const s of book.SheetNames)incoming.push(...parseSheet(s,book.Sheets[s]));incoming=dedupe(incoming);if(!incoming.length)throw new Error("找不到可匯入資料。系統會自動搜尋姓名／Type／編號所在的欄位列。");let added=0,updated=0;write(existing=>{const map=new Map(existing.map(x=>[keyOf(x),x]));for(const x of incoming){const k=keyOf(x);if(map.has(k)){Object.assign(map.get(k),x,{id:map.get(k).id||x.id});updated++;}else{existing.push(x);map.set(k,x);added++;}}});const types=incoming.reduce((a,x)=>{const k=x.type||"Other";a[k]=(a[k]||0)+1;return a},{});alert(`RSVP 匯入完成：${incoming.length} 筆\n新增 ${added} / 更新 ${updated}\n${Object.entries(types).map(([k,v])=>`${k} ${v}`).join(" / ")}`);refresh();}
function statsHTML(rows){const attended=rows.filter(x=>x.attended).length,walkins=rows.filter(x=>x.walkin).length,noShow=Math.max(0,rows.length-attended),rate=rows.length?Math.round(attended/rows.length*1000)/10:0;return`<div class="kpi-grid rsvp-kpis rsvp-kpis-five"><div class="kpi"><span>Total RSVP</span><strong>${rows.length}</strong></div><div class="kpi"><span>Attended</span><strong>${attended}</strong></div><div class="kpi"><span>No Show</span><strong>${noShow}</strong></div><div class="kpi"><span>Walk-in</span><strong>${walkins}</strong></div><div class="kpi"><span>Attendance Rate</span><strong>${rate}%</strong></div></div>`;}
function typeHTML(rows){const counts=rows.reduce((a,x)=>{const k=String(x.type||"Unassigned").trim()||"Unassigned";a[k]=(a[k]||0)+1;return a},{}),items=Object.entries(counts).sort((a,b)=>b[1]-a[1]);return`<div class="rsvp-type-breakdown"><span class="rsvp-breakdown-label">Type Breakdown</span>${items.length?items.map(([k,v])=>`<span class="tag neutral">${esc(k)} <b>${v}</b></span>`).join(""):'<span class="hint">尚無 RSVP 資料</span>'}</div>`;}
function managerHTML(p){const rows=p?.attendees||[];return`<div id="rsvpManagerV4" class="rsvp-manager-v3"><div class="card rsvp-tools-card"><div class="card-title-row"><div><h3>RSVP / Attendance Manager</h3><p class="hint">活動前匯入或手動新增；活動後沿用同一份名單更新出席狀態。</p></div><div class="inline-actions rsvp-main-actions"><label class="button primary small file-button">Import Excel<input id="rsvpImport" type="file" accept=".xlsx,.xls,.csv"></label><button id="rsvpAdd" class="button primary small">+ Add Attendee</button></div></div>${statsHTML(rows)}${typeHTML(rows)}<div class="rsvp-filter-row"><input id="rsvpSearch" placeholder="Search name / company / email / code"><input id="rsvpTypeFilter" list="rsvpTypes" placeholder="Filter Type"><datalist id="rsvpTypes"><option value="Customer"><option value="Partner"><option value="Other"><option value="HPE"></datalist><button id="rsvpClear" class="button small">Clear</button></div></div><div class="card rsvp-batch-card"><div class="card-title-row"><div><h3>Batch Edit</h3><p class="hint">先勾選多筆 RSVP，再一次修改 Type 或出席狀態。</p></div></div><div class="rsvp-batch-row"><strong><span id="rsvpSelectedCount">0</span> selected</strong><input id="rsvpBatchType" list="rsvpTypes" placeholder="輸入 Type（可自訂）"><button class="button small" data-batch="type">Set Type</button><button class="button small" data-batch="attend-yes">Attended ✓</button><button class="button small" data-batch="attend-no">No Show</button><button class="button small" data-batch="walkin-yes">Walk-in ✓</button><button class="button small" data-batch="walkin-no">Not Walk-in</button><button class="button small danger" data-batch="delete">Delete</button></div></div><div class="card"><div class="attendee-table-wrap"><table class="attendee-table rsvp3-table"><thead><tr><th><input id="rsvpAll" type="checkbox"></th><th>編號</th><th>Type</th><th>姓名</th><th>公司</th><th>職稱</th><th>Email</th><th>報名資訊來源</th><th>來源</th><th>Attended</th><th>Walk-in</th></tr></thead><tbody id="rsvpBody"></tbody></table></div></div><div id="rsvpModal" class="modal hidden"><div class="modal-backdrop" data-rsvp-close></div><div class="modal-card"><div class="modal-head"><div><div class="eyebrow">RSVP</div><h2>Add Attendee</h2><p>Type 可直接手動輸入，不限 Customer / Partner / Other / HPE。</p></div><button class="modal-close" data-rsvp-close>×</button></div><form id="rsvpForm" class="form-grid"><div class="field"><label>Type *</label><input name="type" list="rsvpTypesModal" required placeholder="直接輸入 Type"><datalist id="rsvpTypesModal"><option value="Customer"><option value="Partner"><option value="Other"><option value="HPE"></datalist></div><div class="field"><label>編號</label><input name="code"></div><div class="field"><label>姓名 *</label><input name="name" required></div><div class="field"><label>公司</label><input name="company"></div><div class="field"><label>職稱</label><input name="title"></div><div class="field"><label>Email</label><input name="email" type="email"></div><div class="field"><label>行動電話</label><input name="phone"></div><div class="field"><label>報名資訊來源</label><input name="registrationSource"></div><div class="field"><label>來源</label><input name="source"></div><div class="field"><label>備註</label><input name="notes"></div><div class="field"><label><input name="attended" type="checkbox"> Attended</label></div><div class="field"><label><input name="walkin" type="checkbox"> Walk-in</label></div><div class="modal-actions full"><button type="button" class="button" data-rsvp-close>Cancel</button><button class="button primary" type="submit">Add Attendee</button></div></form></div></div></div>`;}
function renderRows(){const p=project(),body=$("#rsvpBody");if(!p||!body)return;const q=norm($("#rsvpSearch")?.value),tf=norm($("#rsvpTypeFilter")?.value),rows=(p.attendees||[]).filter(x=>{const hay=norm([x.code,x.type,x.name,x.company,x.title,x.email,x.registrationSource,x.source].join(" "));return(!q||hay.includes(q))&&(!tf||norm(x.type).includes(tf));});body.innerHTML=rows.length?rows.map(x=>`<tr data-id="${esc(x.id)}"><td><input class="rsvp-select" type="checkbox"></td><td>${esc(x.code)}</td><td><input data-field="type" value="${esc(x.type)}" list="rsvpTypes"></td><td><input data-field="name" value="${esc(x.name)}"></td><td><input data-field="company" value="${esc(x.company)}"></td><td><input data-field="title" value="${esc(x.title)}"></td><td><input data-field="email" value="${esc(x.email)}"></td><td><input data-field="registrationSource" value="${esc(x.registrationSource)}"></td><td><input data-field="source" value="${esc(x.source)}"></td><td class="check-cell"><input data-field="attended" type="checkbox" ${x.attended?"checked":""}></td><td class="check-cell"><input data-field="walkin" type="checkbox" ${x.walkin?"checked":""}></td></tr>`).join(""):`<tr><td colspan="11" class="table-empty">No attendees</td></tr>`;$$('#rsvpBody tr[data-id]').forEach(tr=>{const id=tr.dataset.id;$$('[data-field]',tr).forEach(el=>{el.disabled=!editable();el.onchange=()=>{const field=el.dataset.field;write(arr=>{const x=arr.find(a=>String(a.id)===String(id));if(!x)return;x[field]=el.type==="checkbox"?el.checked:el.value;if(field==="walkin"&&el.checked)x.attended=true;});refresh();};});});$$('.rsvp-select').forEach(x=>x.onchange=updateSelected);updateSelected();}
function selectedIds(){return $$('#rsvpBody tr[data-id]').filter(tr=>$('.rsvp-select',tr)?.checked).map(tr=>tr.dataset.id);}
function updateSelected(){const e=$("#rsvpSelectedCount");if(e)e.textContent=selectedIds().length;}
function batch(action){const ids=new Set(selectedIds());if(!ids.size)return alert("請先勾選名單。");if(!editable())return;const type=String($("#rsvpBatchType")?.value||"").trim();if(action==="type"&&!type)return alert("請輸入要套用的 Type。");if(action==="delete"&&!confirm(`確定刪除 ${ids.size} 筆 RSVP？`))return;write(arr=>{if(action==="delete"){for(let i=arr.length-1;i>=0;i--)if(ids.has(String(arr[i].id)))arr.splice(i,1);return;}arr.forEach(x=>{if(!ids.has(String(x.id)))return;if(action==="type")x.type=type;if(action==="attend-yes")x.attended=true;if(action==="attend-no")x.attended=false;if(action==="walkin-yes"){x.walkin=true;x.attended=true;}if(action==="walkin-no")x.walkin=false;});});refresh();}
function wire(){const imp=$("#rsvpImport");if(imp)imp.onchange=async e=>{const f=e.target.files?.[0];if(!f)return;try{await importWorkbook(f)}catch(err){console.error(err);alert(`RSVP 匯入失敗：${err.message}`)}finally{e.target.value=""}};const add=$("#rsvpAdd");if(add)add.onclick=()=>$("#rsvpModal")?.classList.remove("hidden");$$('[data-rsvp-close]').forEach(x=>x.onclick=()=>$("#rsvpModal")?.classList.add("hidden"));const form=$("#rsvpForm");if(form)form.onsubmit=e=>{e.preventDefault();const f=new FormData(form),walkin=f.get("walkin")==="on",row={id:rid(),type:String(f.get("type")||"").trim(),code:String(f.get("code")||"").trim(),name:String(f.get("name")||"").trim(),company:String(f.get("company")||"").trim(),title:String(f.get("title")||"").trim(),email:String(f.get("email")||"").trim(),phone:String(f.get("phone")||"").trim(),registrationSource:String(f.get("registrationSource")||"").trim(),source:String(f.get("source")||"").trim(),notes:String(f.get("notes")||"").trim(),attended:f.get("attended")==="on"||walkin,walkin};write(a=>a.push(row));form.reset();$("#rsvpModal")?.classList.add("hidden");refresh();};const search=$("#rsvpSearch"),tf=$("#rsvpTypeFilter");if(search)search.oninput=renderRows;if(tf)tf.oninput=renderRows;const clear=$("#rsvpClear");if(clear)clear.onclick=()=>{if(search)search.value="";if(tf)tf.value="";renderRows();};const all=$("#rsvpAll");if(all)all.onchange=()=>{$$('.rsvp-select').forEach(x=>x.checked=all.checked);updateSelected();};$$('[data-batch]').forEach(b=>b.onclick=()=>batch(b.dataset.batch));if(!editable()){$$('#rsvpManagerV4 .button.primary,#rsvpManagerV4 .button.danger,#rsvpManagerV4 [data-batch],#rsvpBatchType').forEach(x=>x.style.display="none");}}
function renderManager(){const active=$$('.nav-item[data-view="rsvp"]').some(x=>x.classList.contains("active"));if(!active)return;const c=$("#formContent"),p=project();if(!c||!p)return;c.innerHTML=managerHTML(p);renderRows();wire();}
const obs=new MutationObserver(()=>{const active=$$('.nav-item[data-view="rsvp"]').some(x=>x.classList.contains("active"));if(active&&!$("#rsvpManagerV4"))setTimeout(renderManager,0);});const root=$("#viewRoot");if(root)obs.observe(root,{childList:true,subtree:true});document.addEventListener("click",e=>{if(e.target.closest?.('.nav-item[data-view="rsvp"]'))setTimeout(renderManager,30)});setTimeout(renderManager,200);